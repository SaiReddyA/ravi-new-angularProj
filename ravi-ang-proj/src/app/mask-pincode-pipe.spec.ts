import { MaskPincodePipe } from './mask-pincode-pipe';

describe('MaskPincodePipe', () => {
  it('create an instance', () => {
    const pipe = new MaskPincodePipe();
    expect(pipe).toBeTruthy();
  });
});
